# Dedicated optional Docling dependency set for Step 0 validation.
# Install into an isolated venv to avoid perturbing your main runtime:
#   python -m venv .venv-docling
#   source .venv-docling/bin/activate
#   pip install -r requirements-docling.txt

docling==2.73.1
docling-core==2.65.1
docling-ibm-models==3.11.0
docling-parse==4.7.3

# Keep core numeric stack compatible with current Cortex baseline.
numpy==1.26.4
